# Quick Task 260704-h1e: Remove legacy Python code and update configuration

## Goal
Remove all unused legacy Python and Django files (`backend/`, `frontend/`, `.venv/`) and remove/update configuration files (`package.json`, `tailwind.config.js`, `render.yaml`) to ensure clean builds and Next.js-only deployments on Vercel.

## Tasks
- [x] Task 1: Delete legacy Python/Django directories (`backend/`, `frontend/`, `.venv/`).
- [x] Task 2: Update configuration files (`package.json`, `tailwind.config.js`) and delete `render.yaml` (since deployment is on Vercel).
- [x] Task 3: Verify the Next.js app builds cleanly.
