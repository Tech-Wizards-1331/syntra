# Quick Task 260704-h1e: Remove legacy Python code and update configuration

## Goal
Remove all unused legacy Python and Django files (`backend/`, `frontend/`, `.venv/`) and update `package.json`, `tailwind.config.js`, and `render.yaml` to ensure clean builds and Next.js-only deployments.

## Tasks
- [x] Task 1: Delete legacy Python/Django directories (`backend/`, `frontend/`, `.venv/`).
- [x] Task 2: Update configuration files (`package.json`, `tailwind.config.js`, `render.yaml`) to remove references to deleted files.
- [x] Task 3: Verify the Next.js app builds cleanly.
