---
quick_id: 260608-uv8
type: execute
files_modified: [
  "app/organizer/dashboard/page.tsx"
]
must_haves:
  truths:
    - "A scan button is displayed for each hackathon in the organizer dashboard."
    - "The scan button links to /organizer/scan?hackathonId=${h.id}."
  artifacts:
    - path: "app/organizer/dashboard/page.tsx"
      provides: "Scan button added to hackathon actions list in the dashboard"
---

# Quick Plan: Add scan button to organizer dashboard hackathon actions

Add a new action button labeled "Scan" next to the Seating, Edit, and Details buttons in the organizer's hackathon list. This button links directly to `/organizer/scan?hackathonId=${h.id}` and passes the specific hackathon's ID as a query parameter.

## Proposed Changes

### app/organizer/dashboard

#### [MODIFY] [page.tsx](file:///c:/Users/ansh/OneDrive/Desktop/Ansh/Sem-4/syntra/app/organizer/dashboard/page.tsx)
- Import the `Scan` icon from `lucide-react` if not already imported (it's already imported at line 11: `Scan,`).
- Add a new `<Link>` next to the "Seating" button inside the actions wrapper:
  ```tsx
  <Link
    href={`/organizer/scan?hackathonId=${h.id}`}
    className="p-2.5 rounded-xl bg-slate-950 border border-slate-850 hover:bg-slate-900 text-slate-400 hover:text-teal-400 transition text-xs font-semibold flex items-center gap-1.5"
  >
    <Scan className="w-3.5 h-3.5" />
    Scan
  </Link>
  ```

## Verification Plan

### Manual Verification
- View the dashboard at `/organizer/dashboard`.
- Verify the "Scan" button is visible for each hackathon.
- Click the button and verify it redirects to `/organizer/scan?hackathonId=<id>` and pre-selects the corresponding hackathon.
