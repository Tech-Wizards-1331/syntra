---
wave: 1
depends_on: []
files_modified:
  - frontend/static/css/design-system.css
autonomous: true
---

# Quick Task: Remove button hover underline and fix hover text visibility

This quick task addresses UI styling issues on button hovers:
1. Reverting `a:hover:not(.btn)` selector back to `a:hover` in `design-system.css` to fix double underlines on navbar links (so standard anchor styling overrides behave normally).
2. Explicitly setting text hover colors for all button subclasses (`.btn--primary:hover`, `.btn--secondary:hover`, `.btn--ghost:hover`, and `.btn--danger:hover`) to prevent anchor-based buttons from inheriting standard link hover styling.

## Verification Criteria
- [ ] Buttons do not get underlined on hover.
- [ ] Blue primary buttons keep white text on hover (text does not turn blue on hover).
- [ ] Navbar links do not have double underlines (the bottom border is visible, but text-decoration underline is not).
- [ ] Secondary, ghost, and danger buttons display correct hover text color.

## must_haves
- Revert `a:hover:not(.btn)` to `a:hover`.
- Define hover colors on button subclasses in `design-system.css`.

---

## Task 1: Update design-system.css
<task>
<read_first>
- frontend/static/css/design-system.css
</read_first>
<action>
- Revert the general `a:hover` selector.
- Add hover state colors to all button classes (`.btn--primary:hover`, `.btn--secondary:hover`, `.btn--ghost:hover`, and `.btn--danger:hover`).
</action>
<acceptance_criteria>
- Specificity issue is fixed.
- No buttons are underlined or lose text visibility on hover.
</acceptance_criteria>
</task>
