# Quick Plan: Configure project for Vercel deployment and Supabase database

## Tasks
1. Update database provider to `postgresql` in `prisma/schema.prisma`.
2. Add `postinstall` script to `package.json` to generate Prisma Client.
3. Validate Prisma schema locally and build the Next.js app to verify compatibility.
