---
wave: 1
depends_on: []
files_modified:
  - frontend/templates/accounts/complete_profile.html
autonomous: true
---

# Quick Task: Complete Profile Page Wrapper

This quick task wraps both the nav and main content of the complete profile page inside a `.profile-page-wrapper` div element for improved layout control.

## Verification Criteria
- [ ] Both `<nav>` and `<main>` tags are nested inside `.profile-page-wrapper`.
- [ ] Script tags remain outside `.profile-page-wrapper`.
- [ ] The profile page functions normally and styling is preserved.

## must_haves
- Wrap both `<nav>` and `<main>` inside a single `div` element with class `.profile-page-wrapper`.

---

## Task 1: Add profile page wrapper div
<task>
<read_first>
- frontend/templates/accounts/complete_profile.html
</read_first>
<action>
In `frontend/templates/accounts/complete_profile.html`:
1. Add `.profile-page-wrapper` definition in `<style>`:
   ```css
   .profile-page-wrapper {
       display: flex;
       flex-direction: column;
       min-height: 100vh;
   }
   ```
2. Insert `<div class="profile-page-wrapper">` immediately after `<body>` (line 76).
3. Insert `</div>` immediately after the `</main>` closing tag (line 257).
</action>
<acceptance_criteria>
- The HTML validates and has correct matching open/close tags.
- Page displays and behaves correctly.
</acceptance_criteria>
</task>
